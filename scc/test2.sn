val print_i32 : (i32) -> i32;


val a = 10;

val b = fun (a, c) -> -c + 5;

val bar = fun (n) -> n+2;

fun foo() i32 {
	# print("It is so nice, to be alive.");
	print_i32(5);
	10
}

val main = {
	val test = 32;
	val c = test * test;
	var a = 0;
	print_i32(a);

	val plus2 = fun (n) -> n + 2;
	val three = plus2(1);
	print_i32(three);

	{
		val a = 10;
		c = 3;
		0
	};
	
	print_i32(a);
	print_i32(c);
	0
};
