# constants
val x = 1;
val y = 2;

###
multiline comment ##
do extend until the next matching sequence
###

val main = fun () -> {
	val a : i32 = 3;
	val b : i32 = 10;
	print_i32( @add_i32(a,b));
	return 0;
}
