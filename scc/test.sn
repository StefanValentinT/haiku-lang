# constants
val x = 1;
val y = 2;

###
multiline comment ##
do extend until the next matching sequence
###

val a # declares a without type nor init

val b : i32 #declares b only with its type

val t = fun (a, b) -> @add_i32(a, b); # declaration wiht init

val r : i32 = 0; # declaration with type and init

type int = i32;

val main = fun () -> {
	val a : i32 = 3;
	val b : i32 = 10;
	print_i32( @add_i32(a,b));
	return 0;
}
